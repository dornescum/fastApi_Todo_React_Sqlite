# pip install fastapi
 
# pip install "uvicorn[standard]"
 


## uvicorn main:app --reload

- create table todos
- (
    id     INTEGER not null primary key,
    item   VARCHAR,
    isDone INTEGER
- );
